/**
 * AxonClaw - Skills View
 * 技能管理界面 - 使用真实 Skills 页面（OpenClaw workspace + ClawHub）
 */
import React from 'react';
import { Skills } from '@/pages/Skills';

const SkillsView: React.FC = () => {
  return <Skills />;
};

export { SkillsView };
export default SkillsView;
